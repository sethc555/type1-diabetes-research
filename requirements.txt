# within-host models + corpus tooling
numpy>=1.24
scipy>=1.10
matplotlib>=3.6
# scan: ../_scan/scan_all.py uses only the Python stdlib (urllib) for the Semantic Scholar
# Graph API; set S2_API_KEY in the environment to rebuild the corpus.
